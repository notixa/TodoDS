<Window x:Class="TodoDS.TimePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择时间"
        Width="300"
        Height="220"
        Background="{StaticResource BrushSurface}"
        FontFamily="Segoe UI"
        FontSize="13"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Border Style="{StaticResource CardBorder}" Padding="16" Margin="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="设置截止时间"
                       FontSize="15"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource BrushText}"
                       Margin="0,0,0,8" />

            <StackPanel Grid.Row="1" Margin="0,0,0,8">
                <TextBlock Text="日期" Foreground="{StaticResource BrushMuted}" />
                <DatePicker x:Name="DatePicker"
                            Margin="0,4,0,0"
                            SelectedDateFormat="Short" />
            </StackPanel>

            <StackPanel Grid.Row="2" Margin="0,0,0,8">
                <TextBlock Text="时间 (HH:mm)" Foreground="{StaticResource BrushMuted}" />
                <TextBox x:Name="TimeBox" Style="{StaticResource FieldBox}" Margin="0,4,0,0" />
            </StackPanel>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="跳过"
                        Style="{StaticResource SecondaryButton}"
                        Width="64"
                        Click="SkipButton_OnClick" />
                <Button Content="确定"
                        Style="{StaticResource PrimaryButton}"
                        Width="64"
                        Margin="8,0,0,0"
                        Click="ConfirmButton_OnClick" />
            </StackPanel>
        </Grid>
    </Border>
</Window>
