<Window x:Class="TodoDS.PetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        AllowsTransparency="True"
        Background="Transparent"
        Width="240"
        Height="360"
        FontFamily="Segoe UI"
        WindowStartupLocation="Manual">
    <Border Background="#1B1F2A" CornerRadius="16" Padding="12" MouseLeftButtonDown="Border_OnMouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="16" ShadowDepth="6" Opacity="0.25" />
        </Border.Effect>
        <DockPanel LastChildFill="True">
            <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button x:Name="BackButton"
                        Content="← 返回"
                        Width="64"
                        Height="24"
                        Margin="0,0,8,0"
                        Style="{StaticResource GhostButtonDark}"
                        Visibility="Collapsed"
                        Click="BackButton_OnClick" />
                <StackPanel Grid.Column="1">
                    <TextBlock x:Name="ListTitleText"
                               Text="TODO"
                               Foreground="#9AD7FF"
                               FontSize="12"
                               FontWeight="Bold" />
                    <TextBlock x:Name="ListSubtitleText"
                               Text="桌面置顶"
                               Foreground="#8B93A7"
                               FontSize="10" />
                </StackPanel>
                <Button Grid.Column="2"
                        Content="×"
                        Width="24"
                        Height="24"
                        Style="{StaticResource GhostButtonDark}"
                        HorizontalAlignment="Right"
                        Click="CloseButton_OnClick" />
            </Grid>

            <TextBlock x:Name="EmptyText"
                       Text="暂无待办"
                       Foreground="#B0B7C3"
                       Margin="2,8,0,0" />

            <ListBox x:Name="ItemsHost"
                     Background="Transparent"
                     BorderThickness="0"
                     SelectionChanged="ItemsHost_OnSelectionChanged">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#222836" CornerRadius="8" Padding="8" Margin="0,0,0,6">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Opacity" Value="1" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Completed}" Value="True">
                                            <Setter Property="Opacity" Value="0.5" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Title}"
                                               Foreground="#F5F5F5"
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="TextDecorations" Value="{x:Null}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Completed}" Value="True">
                                                        <Setter Property="TextDecorations" Value="Strikethrough" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    <TextBlock Text="{Binding DueDisplay}"
                                               Foreground="#8B93A7"
                                               FontSize="11"
                                               Margin="0,2,0,0" />
                                </StackPanel>
                                <Grid Grid.Column="1" VerticalAlignment="Center">
                                    <CheckBox Click="TodoCheck_OnClick"
                                              IsChecked="{Binding Completed, Mode=TwoWay}"
                                              Margin="8,0,0,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Right">
                                        <CheckBox.Style>
                                            <Style TargetType="CheckBox">
                                                <Setter Property="Focusable" Value="False" />
                                                <Setter Property="Visibility" Value="Visible" />
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="CheckBox">
                                                            <Border x:Name="Box"
                                                                    Width="16"
                                                                    Height="16"
                                                                    CornerRadius="3"
                                                                    Background="#1B2230"
                                                                    BorderBrush="#4A5568"
                                                                    BorderThickness="1">
                                                                <Path x:Name="Mark"
                                                                      Data="M 3 8 L 7 12 L 13 4"
                                                                      Stroke="#9AD7FF"
                                                                      StrokeThickness="2"
                                                                      StrokeEndLineCap="Round"
                                                                      StrokeStartLineCap="Round"
                                                                      Visibility="Collapsed" />
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsChecked" Value="True">
                                                                    <Setter TargetName="Mark" Property="Visibility" Value="Visible" />
                                                                    <Setter TargetName="Box" Property="Background" Value="#243044" />
                                                                    <Setter TargetName="Box" Property="BorderBrush" Value="#6FA8FF" />
                                                                </Trigger>
                                                                <Trigger Property="IsEnabled" Value="False">
                                                                    <Setter TargetName="Box" Property="Opacity" Value="0.5" />
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsList}" Value="True">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </CheckBox.Style>
                                    </CheckBox>
                                    <TextBlock Text="›"
                                               Foreground="#9AD7FF"
                                               FontSize="16"
                                               VerticalAlignment="Center"
                                               HorizontalAlignment="Right">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsList}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Grid>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </DockPanel>
    </Border>
</Window>
